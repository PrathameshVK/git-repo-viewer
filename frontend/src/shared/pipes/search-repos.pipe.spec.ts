import { SearchReposPipe } from './search-repos.pipe';

describe('SearchReposPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReposPipe();
    expect(pipe).toBeTruthy();
  });
});
